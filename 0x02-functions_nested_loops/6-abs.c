#include "main.h"

/**
 * _abs-determines the absolute value of an integer
 * @ab: number take by the function
 * Returns: an absolute value of a number
 */

int _abs(int ab)
{
	if (ab < 0)
	{
	return ((ab * ((ab > 0) - (ab < 0)));
	}
}
